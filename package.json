{
  "name": "propane-fuel-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "ci": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mailersend": "^3.2.0",
    "playwright": "^1.35.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
